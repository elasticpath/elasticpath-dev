{
  "$schema": "https://turbo.build/schema.json",
  "extends": [
    "//"
  ],
  "tasks": {
    "build": {
      "outputs": [
        "public/**",
        ".cache/**",
        ".vercel/**",
        "build/**"
      ],
      "env": [
        "NODE_ENV"
      ]
    }
  }
}
